Malloy semantic-layer analytics for {{INSTANCE_NAME}}. Start with `list_sources` or `describe_source` — every tool result carries the workflow and points you to `yo_help`. If you can run shell commands, `fetch_compiled_model` first and compile queries locally with `malloyyo_client`: it holds the whole model, so drafts compile in milliseconds instead of a round trip each.
